{
  "name": "presentation",
  "type": "module",
  "scripts": {
    "dev": "marp --watch --server --html --theme themes/*.css -I .",
    "build": "marp --html --theme themes/*.css",
    "start": "marp --server -I ."
  },
  "dependencies": {
    "@marp-team/marp-cli": "^3.4.0"
  }
}
